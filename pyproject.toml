[project]
name = "my_app"
version = "0.1.0"
description = ""
authors = [
    {name = "nikolay",email = "nik.fss@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.14,<4.0"
dependencies = [
    "textual (>=8.2.8,<9.0.0)",
    "httpx (>=0.28.1,<0.29.0)"
]




[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"


[tool.poetry.scripts]
monitoring = "my_app.cli:run"