[project]
name = "monitoring"
version = "0.1.0"
description = ""
authors = [
    {name = "nikolay",email = "nik.fss@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.14,<4.0"
dependencies = [
    "textual (>=8.2.8,<9.0.0)"
]

[tool.poetry]
packages = [{include = "monitoring", from = ".."}]

[tool.poetry.scripts]
monitoring = "monitoring.cli:run"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
